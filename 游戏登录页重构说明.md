# 游戏登录页面账号登录功能重构说明

## 📌 版本信息
- **重构日期**: 2026-08-10
- **涉及组件**: GameLogin.vue, game.html, wrangler._worker.js
- **核心功能**: 支持微信小程序环境（潘桃园等特殊活动）

---

## 🎯 问题背景

### 原问题
1. 蟠桃园等特定活动中，游戏客户端检测到 `wx_mini_1`平台或`servicewechat.com` 域名
2. 当前模拟器使用通用 Android WebView UA，无法通过活动校验
3. Cloudflare Workers 缺少微信小程序服务域名的代理配置

---

## ✅ 重构方案

### 1. SDK Mock 增强 (game.html)
**位置**: `public/game.html` - SDK Mock 部分

**新增功能**:
```javascript
// 自动检测微信小程序环境
function isPanTaoOrWeChatMini() {
  // 检查 URL 参数中的 platformExt
  if (platformExt === 'wx_mini_1' || 
      platformExt === 'wxmini' || 
      platform === 'wechat' ||
      window.location.host.includes('servicewechat.com')) {
    return true;
  }
}

// 根据环境返回不同的设备信息
getSystemInfo: function() { 
  if (isWeChatMini) {
    // 微信小程序专用：iOS + iPhone 13 Pro + MicroMessenger/8.0.3
    return {
      system: 'iOS',
      platform: 'wechat',
      model: 'iPhone 13 Pro',
      SDKVersion: '3.3.0',
      wechatVersion: '8.0.3',
      // ... 完整 iOS 设备信息
    };
  } else {
    // 通用 Android 环境
    return { platform: 'android', ... };
  }
}
```

**触发条件**:
- 从游戏启动 URL 中传递 `platformExt=wx_mini_1` 参数
- 或访问地址包含 `servicewechat.com`

---

### 2. Cloudflare Workers 代理增强 (wrangler._worker.js)
**位置**: `wrangler._worker.js`

**新增代理路径**:
| 前缀 | 目标域名 | 用途 |
|------|----------|------|
| `/api/wechat` | `servicewechat.com` | 微信小程序服务（蟠桃园核心） |
| `/api/wxmini` | `appservice.qq.com` | QQ 小程序服务 |

**UA 配置示例**:
```javascript
// servicewechat.com 代理配置
headers: {
  'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 15_6 like Mac OS X)...',
  'Host': 'servicewechat.com',
  'X-WeApp': 'xyzw'
}
```

**部署命令**:
```bash
npx wrangler pages deploy dist --branch=production \
  --project-name=xyzw-web-helper \
  --commit-dirty=true
```

---

### 3. 登录流程参数传递 (GameLogin.vue)
**位置**: `src/views/GameLogin.vue` - `loginInIframe()` 函数

**变更**:
```javascript
// 旧逻辑
const gameUrl = `${window.location.origin}/game.html?token=${encodeURIComponent(token.id)}`;

// 新逻辑 - 携带 platformExt 参数
const gameUrl = `${window.location.origin}/game.html?` +
                `token=${encodeURIComponent(token.id)}&` +
                `platformExt=${encodeURIComponent(loginData.platformExt || 'mix')}`;
```

**效果**:
- 当 Token 的 `platformExt='wx_mini_1'`时，游戏会自动识别为微信小程序环境
- 无需手动修改登录页面配置

---

## 🔧 使用说明

### 场景 1: 普通安卓登录 (默认)
正常导入 BIN Token → 点击"登录"按钮
- 系统自动检测 `platformExt`字段（默认为`mix`）
- 游戏使用 Android 环境模拟

### 场景 2: 微信小程序环境 (潘桃园等)

#### 方法 A: 导入时为 wx_mini_1平台 Token
1. 在 TokenImport 页面导入微信小游戏 Token
2. BIN 解析后的 `platformExt` 自动为 `wx_mini_1`
3. 登录时 game.html 自动切换到微信小程序模式

#### 方法 B: 手动指定平台 (高级用法)
如需手动设置 platformExt，可在本地 dev 模式下:
```
http://localhost:3000/game.html?token=xxx&platformExt=wx_mini_1
```

### 场景 3: Cloudflare 代理测试
访问以下域名验证代理是否生效:
- `https://your-domain.workers.dev/api/wechat/...` → servicewechat.com
- `https://your-domain.workers.dev/api/wxmini/...` → appservice.qq.com

---

## ⚙️ 配置文件修改清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `wrangler._worker.js` | 新建 | 新增微信小程序代理服务 |
| `public/game.html` | 修改 | SDK Mock 增加微信小程序环境检测 |
| `src/views/GameLogin.vue` | 修改 | iframe URL 传递 platformExt 参数 |
| `dist/game.html` | 同步 | 生产构建时自动生成 |

---

## 🧪 测试步骤

### 1. 本地测试
```bash
npm.cmd run dev
```
访问：`http://localhost:3000/`
- 导入任意 Token
- 点击"登录"按钮
- 打开开发者工具 Console，查看日志:
  ```
  [SDK Mock] 微信小程序环境：false  // 普通环境
  [SDK Mock] 微信小程序环境：true   // wx_mini_1 环境
  ```

### 2. 微信小程序环境测试
如果 BIN Token 包含 `platformExt=wx_mini_1`:
- 登录成功后 Console 应显示：
  ```
  [SDK Mock] 微信小程序环境：true
  [GameLogin] SDK Mock 初始化完成
  ```

### 3. 线上测试
部署到 Cloudflare Pages 后:
```bash
npx.cmd wrangler pages deploy dist --branch=production \
  --project-name=xyzw-web-helper --commit-dirty=true
```

---

## 🐛 常见问题

### Q1: 为什么蟠桃园仍然提示"环境异常"?
**排查步骤**:
1. 检查 game.html Console 是否输出 `[SDK Mock] 微信小程序环境：true`
2. 检查 Token 的 `platformExt` 是否为 `wx_mini_1`
3. 确认 Cloudflare Workers 已部署并启用代理
4. 清除浏览器缓存强制刷新 (Ctrl+Shift+R)

### Q2: 如何临时切换到微信小程序模式？
**临时方案**:
直接访问游戏页面并添加参数:
```
http://localhost:3000/game.html?platformExt=wx_mini_1
```

### Q3: Cloudflare Workers 代理未生效？
**解决方案**:
1. 确认 `wrangler.toml`配置了正确的路由:
   ```toml
   [[routes]]
   pattern = "workers.your-domain.com/*"
     custom_handler = true
   ```
2. 检查请求 URL 是否包含`/api/wechat`前缀
3. 查看 Workers Logs 确认代理逻辑是否执行

---

## 📝 注意事项

1. **Token 兼容性**: 该功能仅对带有 `platformExt`字段的 BIN Token 生效
2. **Cloudflare 部署**: 必须同时部署 `_worker.js`和`dist/`目录
3. **浏览器缓存**: 更改 platformExt 可能需要清除缓存才能生效
4. **混合环境**: 同一浏览器可同时运行多个 Token，每个独立检测环境

---

## 🔄 回退方案

如需恢复旧版行为:
1. 删除 `wrangler._worker.js`中新增的`/api/wechat`和`/api/wxmini` 代理
2. 回滚 `game.html` 至无环境检测的版本
3. 回滚 `GameLogin.vue`移除 `platformExt`参数传递

---

## 📄 相关资源

- **原始需求分析**: `蟠桃修复分析 -20260804.md` (需找回)
- **游戏定义文件**: `game-defines.js` (版本信息)
- **原有 Worker 配置**: `_worker.js` (参考用)

---

## 🚀 后续优化建议

1. **UI 层面**: 在 Token 列表中显示 platformExt 标识
2. **批量操作**: 支持按 platformExt 筛选 Token
3. **自动适配**: 根据活动类型自动切换环境模式
4. **诊断工具**: 增加环境检测状态显示面板
