# 阵容助手（Unlimitedlineup）功能实现文档

## 一、功能概述

阵容助手是一个全功能的阵容管理组件，支持保存/应用/导入/导出阵容，并提供英雄编辑、更换、无损换将、装备洗练查看、俱乐部科技同步等完整能力。

| 功能模块 | 说明 |
|----------|------|
| 刷新数据 | 从服务端拉取最新阵容、英雄、鱼灵、珍珠、装备数据 |
| 保存阵容 | 将当前阵容（含鱼灵/科技/装备/宠物/赐福/水晶等）快照保存到本地 |
| 应用阵容 | 一键将保存的阵容完整还原到游戏中（含站位/等级/鱼灵/科技/玩具/宠物/赐福/淬炼翻面/水晶） |
| 阵容槽位切换 | 快速切换 1~6 号阵容槽 |
| 编辑阵容 | 拖拽调整站位、更换武将、无损换将、下阵英雄 |
| 上阵英雄 | 从全英雄库中选择英雄上阵（支持品质/国家/关键词筛选） |
| 装备洗练查看 | 弹窗查看每个英雄4个装备部位的淬炼孔位详情 |
| 导入/导出 | JSON 格式阵容配置的导入导出，支持多格式兼容 |
| 速度控制 | 可调节阵容应用时的指令发送速度（快速/默认/慢速） |
| 俱乐部科技 | 查看保存的科技配置，应用阵容时自动同步科技 |

---

## 二、涉及文件

| 文件路径 | 作用 |
|----------|------|
| `components/cards/Unlimitedlineup.vue` | 阵容助手主组件（4116行） |
| `components/Team/TeamFormation.vue` | 日常页简化版阵容展示卡片 |
| `components/Team/TeamStatus.vue` | 身份牌内嵌阵容展示（含段位系统） |
| `utils/HeroList.js` | 英雄字典、鱼灵映射、珍珠映射、科技配置、淬炼颜色等 |

---

## 三、核心数据结构

### 3.1 响应式状态

```javascript
const loading = ref(false);
const switchingTeamId = ref(null);
const currentTeamId = ref(1);
const availableTeams = ref([1, 2, 3, 4, 5, 6]);
const currentTeamInfo = ref(null);          // 当前阵容槽 teamInfo
const presetTeamData = ref(null);           // presetteam_getinfo 完整响应
const savedLineups = ref([]);               // 已保存的阵容列表（localStorage 持久化）
const allHeroesData = ref({});              // role_getroleinfo 返回的 heroes 对象
const roleHeroesData = ref({});             // 同上，用于查找英雄属性
const editingTeamHeroes = ref({});          // 编辑中的阵容（position → heroObj）
const artifactBooks = ref({});              // 鱼灵图鉴
const pearlMap = ref({});                   // 珍珠数据
const lineupSpeedFactor = ref(1.0);         // 指令发送速度系数
```

### 3.2 已保存阵容数据结构

```javascript
{
  id: "32位随机字符串",
  name: "阵容1 - 14:30:00",
  teamId: 1,                    // 阵容槽位编号
  savedAt: 1718000000000,       // 保存时间戳
  applying: false,              // 是否正在应用中
  heroes: [{
    position: 0,                // 站位 0~4
    heroId: 10101,
    level: 4000,
    attachmentUid: null,
    fishId: 101,                // 鱼灵ID
    pearlId: 201,               // 珍珠ID
    skillId: 301,               // 珍珠技能ID
    slotMap: { ... },           // 珍珠孔位数据
    curQuenchs: [0, 1, 2, 0],  // 4个装备部位的淬炼翻面ID
    equips: [{                  // 完整装备淬炼数据
      enchantUId: 123,
      part: 1,
      quenches: [{ colorId, attrId, attrNum }],
      quenches2: [...]
    }],
    trumpId: 0,                 // 水晶（玩具翻面）ID
    trumpId2: 0,
    power: 1500000000,
    attack: 800000000,
    hp: 2000000000,
    speed: 1500
  }],
  legionResearch: { 101: 5, 102: 3, ... },  // 俱乐部科技等级
  weaponId: 1,                   // 玩具ID
  weaponLevel: 30,               // 玩具等级
  petUId: "xxx",                 // 宠物唯一ID
  enchantMap: { "uid1": "uid2" }, // 赐福关系映射
  artifactBooks: { ... },        // 鱼灵图鉴快照
  pearlMap: { ... },             // 珍珠快照
  presetTeamInfo: { ... }        // 预设阵容快照
}
```

---

## 四、功能按钮一览

### 4.1 工具栏按钮

| 按钮 | 功能 | 调用方法 |
|------|------|----------|
| 刷新数据 | 拉取最新游戏数据 | `refreshTeamInfo()` |
| 保存阵容 | 保存当前阵容快照 | `saveCurrentLineup()` |
| 上阵英雄 | 打开英雄选择弹窗 | `openAddHeroModal()` |
| 已保存阵容 (N) | 显示保存数量 | 纯展示 |

### 4.2 速度控制

```javascript
const speedPresets = [
  { label: "快速", value: 0.3, color: "#4caf50" },   // ~150ms 间隔
  { label: "默认", value: 1.0, color: "#1677ff" },   // ~500ms 间隔
  { label: "慢速", value: 2.0, color: "#ff9800" },   // ~1000ms 间隔
];
// 滑块范围: 0.1 ~ 2.0，步进 0.05
const cmdDelay = computed(() => Math.max(50, Math.round(COMMAND_DELAY * lineupSpeedFactor.value)));
```

### 4.3 阵容卡片操作按钮

| 按钮 | 功能 |
|------|------|
| 重命名 | 弹窗修改阵容名称 |
| 科技 | 查看俱乐部科技详情 |
| 删除 | 删除已保存阵容 |
| 应用 | 一键还原阵容到游戏 |

### 4.4 编辑区英雄操作按钮

| 按钮 | 功能 | 方法 |
|------|------|------|
| 更换 | 选择新武将替换当前位置 | `openExchangeModal(hero)` |
| 无损 | 无损换将（hero_exchange 指令） | `openLosslessModal(hero)` |
| 下阵 | 将英雄从阵容移除 | `removeHero(hero)` |

---

## 五、核心逻辑实现

### 5.1 刷新数据

API 调用链：
1. `presetteam_getinfo` → 获取阵容槽信息
2. 切换到相邻槽再切回（触发服务端数据刷新）
3. `presetteam_getinfo` → 再次获取最新数据
4. `role_getroleinfo` → 获取英雄/鱼灵/珍珠等完整数据

```javascript
const refreshTeamInfo = async () => {
  // 3秒防抖
  const now = Date.now();
  if (now - lastRefreshTime < REFRESH_DEBOUNCE) return;
  lastRefreshTime = now;

  // 1. 获取阵容信息
  let presetTeamResult = await tokenStore.sendMessageWithPromise(tokenId, "presetteam_getinfo", {});

  // 2. 切换到相邻槽再切回（强制刷新服务端数据）
  const otherTeamId = availableTeamIds[currentIndex === 0 ? 1 : currentIndex - 1];
  if (otherTeamId !== targetTeamId) {
    await tokenStore.sendMessageWithPromise(tokenId, "presetteam_saveteam", { teamId: otherTeamId });
    await delay(cmdDelay.value);
    await tokenStore.sendMessageWithPromise(tokenId, "presetteam_saveteam", { teamId: targetTeamId });
    await delay(cmdDelay.value);
  }

  // 3. 重新获取
  presetTeamResult = await tokenStore.sendMessageWithPromise(tokenId, "presetteam_getinfo", {});

  // 4. 获取角色完整数据
  const roleInfo = await tokenStore.sendMessageWithPromise(tokenId, "role_getroleinfo", {});
  roleHeroesData.value = role?.heroes || {};
  artifactBooks.value = role?.artifactBooks || {};
  pearlMap.value = role?.pearlMap || {};
};
```

### 5.2 保存阵容

保存时会完整记录当前阵容的所有维度数据：

```javascript
const saveCurrentLineup = async () => {
  // 1. role_getroleinfo → 获取科技/鱼灵/珍珠/赐福/宠物/水晶
  const roleInfo = await tokenStore.sendMessageWithPromise(tokenId, "role_getroleinfo", {});
  const legionResearch = role?.legionResearch || {};
  const enchantMap = role?.enchantMap || {};
  const petUId = role?.pet?.petUId || null;

  // 2. presetteam_getinfo → 获取玩具ID
  const presetTeamResult = await tokenStore.sendMessageWithPromise(tokenId, "presetteam_getinfo", {});
  const weaponId = teamData?.weapon?.weaponId || null;

  // 3. 构建英雄数据（含鱼灵/珍珠/淬炼/装备/水晶）
  const heroesData = editingHeroes.value.map((hero) => ({
    position, heroId, level, fishId, pearlId, skillId,
    slotMap, curQuenchs, equips, trumpId, power, attack, hp, speed
  }));

  // 4. 存入 savedLineups
  savedLineups.value.unshift({ id, name, heroes, teamId, savedAt, ... });
  saveLineupsToStorage(); // 持久化到 localStorage
};
```

### 5.3 应用阵容（核心复杂逻辑）

`applyLineup()` 是整个组件最复杂的方法，包含以下子步骤：

| 步骤 | 说明 | 涉及 API |
|------|------|----------|
| 1. 鱼灵 attachment 处理 | 确保目标英雄的 attachmentUid 正确归属 | `hero_gointobattle`, `hero_exchange` |
| 2. 下阵不需要的英雄 | 移除阵容中多余的英雄 | `hero_gobackbattle` |
| 3. 上阵目标英雄 | 将目标英雄放到正确站位 | `hero_gointobattle` |
| 4. 调整站位 | 交换位置不正确的英雄 | `hero_gobackbattle` + `hero_gointobattle` |
| 5. 等级还原 | 将英雄等级调整到保存时的值 | `hero_rebirth`, `hero_heroupgradelevel`, `hero_heroupgradeorder` |
| 6. 鱼灵配置 | 加载鱼灵和珍珠技能 | `artifact_unload`, `artifact_load`, `pearl_exchangeskill`, `pearl_replaceskill` |
| 7. 科技同步 | 还原俱乐部科技等级 | `legion_resetresearch`, `legion_research` |
| 8. 玩具切换 | 切换到保存的玩具 | `lordweapon_changedefaultweapon` |
| 9. 宠物切换 | 切换到保存的宠物 | `pet_load` |
| 10. 水晶切换 | 切换到保存的水晶翻面 | `trump_change` |
| 11. 赐福恢复 | 还原装备赐福关系 | `equipment_cancelenchant`, `equipment_enchant` |
| 12. 淬炼翻面 | 还原装备淬炼翻面状态 | `equipment_changequench` |
| 13. 装备完整性校验 | 检查装备是否匹配保存数据 | 校验 + 提示 |

### 5.4 等级还原逻辑

```javascript
const applyHeroLevel = async (tokenId, heroId, targetLevel, currentLevel, currentOrder, slot) => {
  // 如果当前等级 > 目标等级：下阵 → 重生 → 上阵
  if (actualCurrentLevel > targetLevel) {
    await hero_gobackbattle({ slot });
    await hero_rebirth({ heroId });       // 重生回到1级
    await hero_gointobattle({ heroId, slot });
  }

  // 如果阶位不够：先升阶
  if (actualCurrentOrder < expectedOrder) {
    await hero_heroupgradeorder({ heroId });
  }

  // 循环升级（支持批量升级 50/10/5/1）
  while (actualCurrentLevel < targetLevel) {
    const nextOrderLevel = getNextOrderLevel(actualCurrentLevel);
    const stepLimit = Math.min(nextOrderLevel - currentLevel, targetLevel - currentLevel);
    const upgradeNum = [50, 10, 5, 1].find(n => n <= stepLimit) || 1;
    await hero_heroupgradelevel({ heroId, upgradeNum });
    // 到达升阶阈值时自动升阶
    if (actualCurrentLevel >= nextOrderLevel) {
      await hero_heroupgradeorder({ heroId });
    }
  }
};
```

等级阈值表（level → order）：
```
100→1, 200→2, 300→3, 500→4, 700→5, 900→6, 1100→7, 1300→8,
1500→9, 1800→10, 2100→11, 2400→12, 2800→13, 3200→14, 3600→15,
4000→16, 4500→17, 5000→18, 5500→19
```

### 5.5 科技同步

```javascript
const syncLegionResearch = async (tokenId, targetResearch) => {
  // 1. 比较当前科技与目标科技差异
  // 2. 需要调整的类别先执行 legion_resetresearch（重置）
  // 3. 按类别顺序执行 legion_research（逐级升级或一键满级）
  for (const type of sortedTypes) {
    for (const techId of LEGION_TECH_TYPE_MAP[type]) {
      if (targetLevel >= maxLevel) {
        await legion_research({ isMax: true, researchId: techId });
      } else {
        for (let i = 0; i < targetLevel; i++) {
          await legion_research({ isMax: false, researchId: techId });
        }
      }
    }
  }
};
```

### 5.6 阵容槽切换

```javascript
const switchTeam = async (teamId) => {
  await tokenStore.sendMessageWithPromise(tokenId, "presetteam_saveteam", { teamId });
  currentTeamId.value = teamId;
  await refreshTeamInfo();
};
```

### 5.7 拖拽站位调整

使用 HTML5 原生拖拽 API：
```javascript
const onDrop = (event, targetHero) => {
  // 交换 draggedHero 和 targetHero 的 position
  const draggedHeroData = editingTeamHeroes[draggedPos];
  const targetHeroData = editingTeamHeroes[targetPos];
  editingTeamHeroes[draggedPos] = targetHeroData;
  editingTeamHeroes[targetPos] = draggedHeroData;
};
```

### 5.8 无损换将

```javascript
// 直接调用 hero_exchange 指令，不改变装备/等级
await tokenStore.sendMessageWithPromise(tokenId, "hero_exchange", {
  heroId: exchangeHero.heroId,         // 当前英雄
  targetHeroId: exchangeTargetHeroId,   // 替换目标
});
```

### 5.9 导入/导出

**导出格式**（v2）：
```json
{
  "version": 2,
  "roleId": "角色ID",
  "exportTime": 1718000000000,
  "lineups": [ ... ]
}
```

**导入兼容格式**：
- 格式A（Vue新版导出）: `{ version: 2, roleId, lineups: [...] }`
- 格式B（JS端导出）: `{ "saved_lineups_XXX": [...] }`
- 格式C（直接数组）: `[ ... ]`

---

## 六、API 指令汇总

| 指令 | 用途 | 关键参数 |
|------|------|----------|
| `presetteam_getinfo` | 获取阵容槽信息 | 无 |
| `presetteam_saveteam` | 切换阵容槽 | `teamId` |
| `role_getroleinfo` | 获取角色完整数据 | 无 |
| `hero_gointobattle` | 英雄上阵 | `heroId`, `slot` |
| `hero_gobackbattle` | 英雄下阵 | `slot` |
| `hero_exchange` | 无损换将 | `heroId`, `targetHeroId` |
| `hero_rebirth` | 英雄重生（回1级） | `heroId` |
| `hero_heroupgradelevel` | 英雄升级 | `heroId`, `upgradeNum` |
| `hero_heroupgradeorder` | 英雄升阶 | `heroId` |
| `artifact_load` | 装备鱼灵 | `heroId`, `itemId`, `pearlId` |
| `artifact_unload` | 卸下鱼灵 | `heroId` |
| `pearl_exchangeskill` | 交换珍珠技能 | `pearlId1`, `pearlId2` |
| `pearl_replaceskill` | 替换珍珠技能 | `pearlId`, `skillId` |
| `pearl_unloadskill` | 卸下珍珠技能 | `pearlId` |
| `legion_resetresearch` | 重置俱乐部科技 | `type`, `advanced` |
| `legion_research` | 升级俱乐部科技 | `researchId`, `isMax` |
| `lordweapon_changedefaultweapon` | 切换玩具 | `weaponId` |
| `pet_load` | 切换宠物 | `slotUId: { slot, uId }` |
| `trump_change` | 切换水晶翻面 | `heroId` |
| `equipment_enchant` | 装备赐福 | `oriQuenchId`, `tarHeroId` |
| `equipment_cancelenchant` | 取消赐福 | `enchantUId` |
| `equipment_changequench` | 淬炼翻面 | `heroId`, `part` |

---

## 七、辅助数据映射

### 7.1 装备部位

```javascript
const partMap = { 1: "武器", 2: "铠甲", 3: "头冠", 4: "坐骑" };
```

### 7.2 淬炼属性

```javascript
const attrMap = {
  1: "攻击", 2: "血量", 3: "防御", 4: "速度",
  5: "破甲", 6: "破甲抵抗", 7: "精准", 8: "格挡",
  9: "减伤", 10: "暴击", 11: "暴击抵抗", 12: "爆伤",
  13: "爆伤抵抗", 14: "技能伤害", 15: "免控",
  16: "眩晕免疫", 17: "冰冻免疫", 18: "沉默免疫",
  19: "流血免疫", 20: "中毒免疫", 21: "灼烧免疫",
};
```

### 7.3 英雄品质判断

```javascript
const getHeroQuality = (heroId) => {
  const prefix = Math.floor(heroId / 100);
  if (prefix === 1) return "红将";
  if (prefix === 2) return "橙将";
  if (prefix === 3) return "紫将";
  return "其他";
};
```

### 7.4 装备淬炼相似度算法

用于应用阵容时检测装备是否正确归属：

```javascript
const calcQuenchSimilarity = (savedQuenches, currentQuenches) => {
  let score = 0;
  for (const sq of savedQuenches) {
    for (const cq of currentQuenches) {
      if (sq.attrId === cq.attrId) {
        score += cq.colorId === 6 ? 100 : cq.colorId === 5 ? 30 : 5;  // 颜色权重
        score += Math.min(sq.attrNum || 0, cq.attrNum || 0) * 2;       // 属性值权重
      }
    }
  }
  return score;
};
```

---

## 八、localStorage 持久化

存储键名格式：`saved_lineups_{tokenId}`

```javascript
const loadSavedLineups = () => {
  const key = `saved_lineups_${token.id}`;
  const data = localStorage.getItem(key);
  savedLineups.value = data ? JSON.parse(data) : [];
};

const saveLineupsToStorage = () => {
  const key = `saved_lineups_${token.id}`;
  localStorage.setItem(key, JSON.stringify(savedLineups.value));
};
```

---

## 九、生命周期 & 自动化

```javascript
// 组件挂载时：加载本地存储 + 检查连接状态
onMounted(() => {
  loadSavedLineups();
  if (token && status === "connected") refreshTeamInfo();
});

// Token 切换时：重新加载本地存储 + 刷新数据
watch(() => tokenStore.selectedToken, (newToken) => {
  loadSavedLineups();
  if (status === "connected") refreshTeamInfo();
});

// WebSocket 连接状态变化：自动刷新
watch(wsStatus, (newStatus, oldStatus) => {
  if (newStatus === "connected" && oldStatus !== "connected") {
    setTimeout(() => refreshTeamInfo(), 500);
  }
});
```

---

## 十、配套组件

### 10.1 TeamFormation.vue（日常页阵容展示）

简化版阵容展示，功能包括：
- 显示当前阵容的英雄头像和名称（前排2 + 后排3）
- 阵容槽切换按钮（1~4号）
- 刷新按钮
- 使用 `HERO_DICT` 匹配英雄名称和头像

核心 API：`presetteam_getinfo`、`presetteam_saveteam`

### 10.2 TeamStatus.vue（身份牌阵容展示）

在 TeamFormation 基础上增加了：
- 角色身份卡（头像/名称/等级/战力）
- 战力段位系统（10个段位：初出茅庐 → 无极至尊）
- 炫光边框动画

段位配置：
```
0~100万: 初出茅庐 🌱
100万~1000万: 小有名气 ⚔️
1000万~1亿: 出入江湖 🗡️
1亿~5亿: 纵横四方 🏹
5亿~20亿: 盖世豪杰 ⚡
20亿~40亿: 一方枭雄 👑
40亿~60亿: 睥睨江湖 🔱
60亿~90亿: 独霸天下 ⚜️
90亿~150亿: 不世之尊 💎
150亿+: 无极至尊 🌟
```
