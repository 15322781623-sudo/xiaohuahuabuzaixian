# 蟠桃园进图修复报告

> 版本: v2.48.1 | 策略: wx_mini_1 (微信 PC 小程序伪装)

---

## 一、问题根因

蟠桃园检测机制：服务端识别客户端身份，**H5 客户端会被拒绝进入蟠桃园**，只有微信小程序或 QQ 小程序客户端才允许进入。

Web/EXE 环境中：
- `document.origin = null`（file:// 协议）或 `http://localhost`
- HTTP/WS 请求的 `Origin` 头为空/file:///localhost，被网关/WAF/业务层识别为非官方环境
- `PlatformH5.platformType = "hortor"` 在 manifest 请求中自报 H5 身份

## 二、wx_mini_1 策略规范

| 检查点 | 期望值 |
|--------|--------|
| Origin | `https://servicewechat.com` |
| Referer | `https://servicewechat.com/wx0840558555a454ed/331/page-frame.html` |
| User-Agent | 完整 PC 微信小程序 UA（含 MicroMessenger、MiniProgramEnv、WindowsWechat） |
| xweb_xhr | `1` |
| X-Requested-With | `XMLHttpRequest` |
| manifest platform | `wx`（非 `hortor`） |
| GAME_VERSION | `-wx` 后缀 |
| APPID | `wx0840558555a454ed` |
| GAME_ID | `xyzw_mix` |

## 三、修改清单

### 3.1 引擎身份定义 — game-defines

**文件**: `public/game/game-defines.a175e.js`（及 dist、android/build、123 目录副本）

```javascript
gt.PLATFORM = 'h5'           // 保持 h5 以兼容 CDN 资源路径（改 wx 会破坏资源加载）
gt.APPID = 'wx0840558555a454ed'
gt.GAME_ID = 'xyzw_mix'
gt.GAME_VERSION = '2.29.2-wx'  // 版本后缀 -wx 标识微信身份
```

**关键决策**: PLATFORM 保持 `'h5'` 不变。若改为 `'wx'`，引擎 `_platformExtMapping.wx.ext = "mix"` 会改变 CDN 资源 URL 路径，导致贴图/资源加载失败。

### 3.2 Web/EXE JS 层 — patch.js（4 份）

**文件**: `public/game/patch.js`、`dist/game/patch.js`、`android/.../patch.js`（主APK + 123变体 + build目录）

#### 3.2.1 wx API Mock
模拟微信小游戏 `wx` 对象，防止 `PlatformWX.init()` 调用 `wx.getSystemInfoSync()` 等 API 时崩溃：

```javascript
window.wx = {
    getSystemInfoSync: function() { return { ... }; },
    getDeviceInfo: function() { return { ... }; },
    getSystemInfo: function(opts) { ... },
    getDeviceBenchmarkInfo: function(opts) { ... },
    getStorageInfo: function(opts) { ... },
    getNetworkType: function(opts) { ... },
    // ...
};
```

#### 3.2.2 SDK Mock 返回值
```javascript
window.__HORTOR_SDK__.getClientVersion = function() {
    return GAME_VERSION || '2.29.2-wx';
};
window.__HORTOR_SDK__.getGameId = function() {
    return GAME_ID || 'xyzw_mix';
};
```

#### 3.2.3 XHR Manifest 拦截器
拦截 `LoginService.manifest()` HTTP POST 请求，将请求体中的 `platform: "hortor"` 改写为 `platform: "wx"`：

```javascript
(function hookManifestPlatform() {
    var _origSend = XMLHttpRequest.prototype.send;
    var _origOpen = XMLHttpRequest.prototype.open;
    XMLHttpRequest.prototype.open = function(method, url) {
        this.__url = url;
        return _origOpen.apply(this, arguments);
    };
    XMLHttpRequest.prototype.send = function(body) {
        if (this.__url && this.__url.indexOf('/login/manifest') !== -1 && body) {
            var data = JSON.parse(body);
            if (data.params && data.params.platform === 'hortor') {
                data.params.platform = 'wx';
                body = JSON.stringify(data);
            }
        }
        return _origSend.call(this, body);
    };
})();
```

### 3.3 启动脚本 — boot.js（4 份）

**文件**: `public/game/boot.js`、`dist/game/boot.js`、`android/.../boot.js`

manifest URL 的查询参数 `platform=hortor` 改为 `platform=wx`：

```javascript
var manifestUrl = 'https://xxz-xyzw.hortorgames.com/login/manifest?platform=wx&version=0.32.0-android';
```

### 3.4 APK Java 原生层 — NativeHttpBridge

**文件**: 
- `android/app/src/main/java/com/xyzw/webhelper/NativeHttpBridge.java`（新建，235行）
- `123/android/app/src/main/java/com/birdking/app/NativeHttpBridge.java`（改造）
- `android/app/src/main/java/com/xyzw/webhelper/MainActivity.java`（注册）

#### 3.4.1 applyWxHeaders() — 按域名补齐请求头

| 目标域名 | Origin | Referer | xweb_xhr | X-Requested-With |
|----------|--------|---------|----------|------------------|
| open.weixin.qq.com | — | https://open.weixin.qq.com/ | — | — |
| comb-platform.hortorgames.com | https://open.weixin.qq.com | https://open.weixin.qq.com/ | — | — |
| xxz-xyzw.hortorgames.com | https://servicewechat.com | .../page-frame.html | 1 | XMLHttpRequest |
| ucenter-app-server.hortorgames.com | https://servicewechat.com | .../page-frame.html | 1 | XMLHttpRequest |
| service-battle.hortorgames.com | scheme://host 动态 | scheme://host/ 动态 | 1 | — |

#### 3.4.2 完整 User-Agent (wxUA)
```
Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko)
Chrome/132.0.0.0 Safari/537.36
MicroMessenger/7.0.20.1781(0x6700143B) NetType/WIFI
MiniProgramEnv/Windows
WindowsWechat/WMPF WindowsWechat(0x63090a13)
UnifiedPCWindowsWechat(0xf2541937) XWEB/19823
```

### 3.5 EXE Tauri 层

**文件**: 
- `src-tauri/tauri.conf.json` — windows.userAgent 设为完整 PC 微信小程序 UA
- `src-tauri/src/lib.rs` — setup() 注入 JS 脚本，Hook XHR/fetch 添加 `xweb_xhr` / `X-Requested-With`

### 3.6 123 变体 game-defines

**文件**: `123/public/game/game-defines.a175e.js`、`123/dist/`、`123/android/`、`123/assets/`

```javascript
gt.PLATFORM = 'h5'
gt.GAME_VERSION = '2.39.3-wx'
```

### 3.7 附带修复：loadBoxButtonStyle dispose 崩溃

**文件**: `public/enhance-scripts/game_enhance_panel.js`（及 dist 副本）

创建 BoxPanel 组件时无 Cocos2d node，直接 dispose 会访问 `this.node.context` 导致 null 崩溃。添加安全校验：

```javascript
if (tempPanel.node && tempPanel.node.isValid) {
    try { tempPanel.dispose(); } catch (_) {}
}
```

## 四、平台能力矩阵

| 检查点 | APK | EXE (Tauri) | Web (浏览器) |
|--------|:---:|:-----------:|:------------:|
| GAME_VERSION -wx | ✅ | ✅ | ✅ |
| APPID / GAME_ID | ✅ | ✅ | ✅ |
| manifest platform→wx | ✅ 原生 + JS | ✅ JS 注入 | ✅ JS 拦截 |
| User-Agent | ✅ NativeHttpBridge | ✅ tauri.conf.json | ❌ 浏览器禁止 |
| Origin 头 | ✅ NativeHttpBridge | ❌ WebView2 控制 | ❌ 浏览器禁止 |
| Referer 头 | ✅ NativeHttpBridge | ❌ WebView2 控制 | ❌ 浏览器禁止 |
| xweb_xhr / X-Requested-With | ✅ NativeHttpBridge | ✅ JS 注入 | ✅ JS 自定义头 |
| WS Origin | ⚠️ 待验证 | ❌ WebView2 | ❌ 浏览器 |

## 五、已知限制

1. **Web 端**: `Origin`/`Referer`/`User-Agent` 是浏览器禁止头，JS 无法修改。需要本地代理转发才能解决。
2. **EXE WebSocket Origin**: WebView2 的 WebSocket 握手 Origin 仍来自 WebView 当前页面，不可从 JS 修改。需要 Rust 层 WebSocket 代理。
3. **EXE HTTP Origin**: 同 WebView2 限制。需通过 Tauri HTTP plugin 代理（Rust 原生 HTTP 可自由设置 Origin）或本地代理方案。

## 六、后续优化方向

1. **EXE 本地代理**（方案 B）: 在 Tauri Rust 层实现 HTTP+WS 双向代理，补齐 Origin/Referer/UA 全套 wx_mini_1 请求头，前端零改动。
2. **Web 端代理**: 因浏览器沙箱硬限制，需独立本地代理进程或 Service Worker 方案。
3. **QQ 小程序备用策略** (qq_mini_2): 如 wx_mini_1 后续被针对性检测，可切换 qq_mini_2 策略。
