# 批量推图核心代码逻辑说明

## 概述

本系统实现了《咸鱼之王》游戏的批量自动化推图功能，支持多账号同时自动闯关、智能 retry 机制、火把道具使用等核心功能。

---

## 一、核心数据结构

### 1. 推图状态对象 (window._pt)

```javascript
window._pt[tid] = {
    running: false,       // 是否正在运行
    stopFlag: false,      // 停止标志
    level: 0,             // 当前关卡
    wins: 0,              // 胜利次数
    losses: 0,            // 失败次数
    retries: 0,           // 连续失败重试次数
    countdown: 0,         // 倒计时（秒）
    totalTime: 0,         // 总战斗时间
    torchAt: 0,           // 火把激活时间戳
    torchDur: 0           // 火把持续时间（毫秒）
}
```

---

## 二、核心函数逻辑

### 1. `_bpPushOnce(tid)` - 单次推图流程

**核心流程：**

```javascript
async _bpPushOnce(tid) {
    // 步骤 1: 获取 Boss 信息
    var _bn = _getBoss(st.level);
    
    // 步骤 2: 计算战斗时长
    var cr = await r.sendMessageWithPromise(tid, "fight_calcleveltime", {}, 15000);
    var j = Number(cr.battleTime) || 300; // 默认 300 秒
    
    // 步骤 3: 等待战斗结束（带心跳包）
    var t0 = Date.now();
    while(st.countdown > 0 && !st.stopFlag) {
        await _bpSleep(1000);
        st.countdown = Math.max(0, Math.ceil(j - (Date.now() - t0) / 1000));
        
        // 每 25 秒发送心跳包保持连接
        if(hb % 25 === 0) {
            try { r.sendMessage(tid, "heart_beat"); } 
            catch(e) {}
        }
        
        // 每 5 秒更新 UI
        if(hb % 5 === 0) _bpUpdCards();
    }
    
    // 步骤 4: 获取战斗结果
    var fr = await r.sendMessageWithPromise(tid, "fight_level", {}, 15000);
    var bd = fr.body || fr;
    var win = bd.success || bd.isWin || false;
    var nl = bd.currLevel || bd.nextLevel || st.level;
    
    // 步骤 5: 处理胜负
    if(win) {
        st.wins++;
        st.retries = 0;
        st.level = nl;
        console.log("✅ 成功！关卡", nl);
    } else {
        st.losses++;
        st.retries++;
        
        // 连续失败 3 次则停止
        if(st.retries >= 3) {
            console.log("❌ 连续失败 3 次，停止");
            st.stopFlag = true;
        } else {
            console.log("❌ 失败，10 秒后重试");
            await _bpSleep(10000);
        }
    }
    
    // 步骤 6: 刷新角色信息
    await r.sendMessageWithPromise(tid, "role_getroleinfo", {}, 8000);
    _bpUpdCards();
}
```

---

### 2. `_bpStartOne(tid)` - 启动单账号推图

**核心流程：**

```javascript
async _bpStartOne(tid) {
    // 验证 WebSocket 连接状态
    if(r.getWebSocketStatus(tid) !== "connected") {
        console.log("正在连接...");
        try {
            var _tk = r.gameTokens.find(x => x.id === tid);
            if(_tk) r.selectToken(tid);
            await _bpSleep(3000);
        } catch(e) {}
        
        if(r.getWebSocketStatus(tid) !== "connected") {
            console.log("连接失败，跳过");
            return;
        }
    }
    
    // 初始化状态
    window._pt[tid] = {
        running: true,
        stopFlag: false,
        level: 0,
        wins: 0,
        losses: 0,
        retries: 0,
        countdown: 0,
        totalTime: 0
    };
    
    console.log("开始推图");
    _bpUpdCards();
    
    // 如果有配置火把，先使用火把
    if(_bpTorch.value) {
        await _bpUseTorch(tid);
    }
    
    // 主循环：持续推图直到停止
    try {
        while(!window._pt[tid].stopFlag) {
            await _bpPushOnce(tid);
            
            if(window._pt[tid].stopFlag) break;
            
            // 检查火把是否过期
            if(_bpTorch.value && window._pt[tid].torchAt) {
                var _te = (Date.now() - window._pt[tid].torchAt) / 1000;
                if(_te >= window._pt[tid].torchDur) {
                    console.log("⚠️ 火把已过期，续用...");
                    await _bpUseTorch(tid);
                }
            }
            
            await _bpSleep(2000); // 两次战斗间隔 2 秒
        }
    } catch(e) {
        console.log("异常:" + e.message);
    } finally {
        window._pt[tid].running = false;
        window._pt[tid].countdown = 0;
        console.log("已停止");
        _bpUpdCards();
    }
}
```

---

### 3. `_bpUseTorch(tid)` - 使用火把道具

**火把类型及效果：**

| 道具 ID | 名称 | 持续时间 | 说明 |
|--------|------|---------|------|
| 1008 | 木材火把 | 10 分钟/个 | 基础加速 |
| 1009 | 青铜火把 | 20 分钟/个 | 中级加速 |
| 1010 | 战神火把 | 30 分钟/个 | 高级加速 |

**实现逻辑：**

```javascript
const _bpUseTorch = async(tid) => {
    var ti = _bpTorch.value;
    if(!ti) return;
    
    var nm = ti === 1008 ? "木材" : 
             ti === 1009 ? "青铜" : 
             ti === 1010 ? "战神" : "";
    
    console.log("使用" + nm + "火把 x10...", "info");
    
    var ok = 0;
    for(var i = 0; i < 10; i++) {
        try {
            await r.sendMessageWithPromise(tid, "item_consume", {
                itemId: ti,
                quantity: 1
            }, 5000);
            ok++;
            await _bpSleep(500);
        } catch(e) {
            console.log("火把第" + (i+1) + "次失败：" + e.message, "error");
            break;
        }
    }
    
    // 计算火把持续时间
    if(window._pt[tid]) {
        window._pt[tid].torchAt = Date.now();
        window._pt[tid].torchDur = (ti === 1008 ? 600 : 
                                   ti === 1009 ? 1200 : 
                                   1800) * ok;
        
        var mins = Math.round((ti === 1008 ? 10 : 
                              ti === 1009 ? 20 : 
                              30) * ok);
        
        console.log(nm + "火把已激活 " + ok + "个 (约" + mins + "分钟)", "success");
    }
};
```

---

### 4. 辅助函数

#### `_bpSleep(ms)` - 延时函数
```javascript
const _bpSleep = ms => new Promise(res => setTimeout(res, ms));
```

#### `_bpLog` - 日志记录
```javascript
const _bpLog = (tid, msg, type="info") => {
    const time = new Date().toLocaleTimeString();
    const log = { time, message: msg, type };
    window._plogs.unshift(log);
    if(window._plogs.length > 500) window._plogs.length = 500;
    _bpUpdLogs();
};
```

#### `_bpUpdCards()` - 更新 UI 卡片
```javascript
const _bpUpdCards = () => {
    const el = document.getElementById("bp-cards");
    if(!el) return;
    
    let h = '';
    (_bpSel.value || []).forEach(tid => {
        const tk = r.gameTokens.find(x => x.id === tid);
        const st = window._pt[tid];
        
        if(st && st.running) {
            h += `<div style="background:#fff;border-radius:6px;padding:8px;margin-bottom:6px">`;
            h += `<div><b>${tk?.name||tid}</b></div>`;
            h += `<div>关卡:${st.level} | 胜利:${st.wins} | 失败:${st.losses}</div>`;
            h += `<div>重试:${st.retries}/3</div>`;
            h += `<div>剩余:${st.countdown}s</div>`;
            h += `</div>`;
        }
    });
    el.innerHTML = h;
};
```

---

## 三、批量控制逻辑

### 1. 批量启动所有选中的账号

```javascript
const _bpStartAll = () => {
    (_bpSel.value || []).forEach(id => {
        if(!window._pt[id] || !window._pt[id].running) {
            _bpStartOne(id);
        }
    });
};
```

### 2. 批量停止所有运行的账号

```javascript
const _bpStopAll = () => {
    (_bpSel.value || []).forEach(id => {
        _bpStopOne(id);
    });
};
```

### 3. 单个账号停止

```javascript
const _bpStopOne = (tid) => {
    if(window._pt[tid]) {
        window._pt[tid].stopFlag = true;
    }
    _bpUpdCards();
};
```

---

## 四、Boss 关卡映射

### BossLevelMapping 结构

```javascript
// 从 boss_level_mapping_fixed.json 加载
window._bossMap = {
    "1": 1,    // 第 1-8 关对应 BOSS 1
    "2": 1,
    ...
    "9": 2,    // 第 9-16 关对应 BOSS 2
    ...
};

function _getBoss(level) {
    for(let key in window._bossMap) {
        if(level >= key && level <= window._bossMap[key]) {
            return window._bossMap[key];
        }
    }
    return null;
}
```

---

## 五、UI 界面组件

### 批量推图控制面板

```html
<div class="push-map-control">
    <!-- 选择账号区域 -->
    <select id="bpSel" multiple>
        <option value="token1">账号 1</option>
        <option value="token2">账号 2</option>
    </select>
    
    <!-- 火把选择 -->
    <select id="bpTorch">
        <option value="0">不使用火把</option>
        <option value="1008">🔥 木材火把 (10min)</option>
        <option value="1009">🔥 青铜火把 (20min)</option>
        <option value="1010">🔥 战神火把 (30min)</option>
    </select>
    
    <!-- 控制按钮 -->
    <button onclick="_bpStartAll()">全部开始</button>
    <button onclick="_bpStopAll()">全部停止</button>
    <button onclick="_bpClearLogs()">清空日志</button>
    
    <!-- 状态卡片展示区 -->
    <div id="bp-cards"></div>
    
    <!-- 日志区域 -->
    <div id="bp-logs"></div>
</div>
```

---

## 六、关键 API 调用

### 主要游戏 API

| API 命令 | 用途 | 超时时间 |
|---------|------|---------|
| `fight_calcleveltime` | 计算战斗所需时间 | 15 秒 |
| `fight_level` | 提交战斗并获取结果 | 15 秒 |
| `role_getroleinfo` | 刷新角色信息 | 8 秒 |
| `item_consume` | 消耗道具（火把） | 5 秒 |
| `heart_beat` | 心跳包保持连接 | - |

---

## 七、错误处理机制

### 1. 连续失败重试策略

```javascript
if(!win) {
    st.losses++;
    st.retries++;
    
    // 达到最大重试次数则停止
    if(st.retries >= 3) {
        st.stopFlag = true; // 停止推图
    } else {
        await _bpSleep(10000); // 等待 10 秒后重试
    }
}
```

### 2. 网络异常处理

```javascript
try {
    // API 调用
} catch(e) {
    st.losses++;
    st.retries++;
    
    if(st.retries >= 3) {
        st.stopFlag = true;
    } else {
        await _bpSleep(10000);
    }
}
```

---

## 八、性能优化

### 1. 连接复用

- 自动检测 WebSocket 连接状态
- 未连接时尝试重新建立连接
- 避免重复连接相同 token

### 2. 批量操作优化

```javascript
// 并行启动多个账号
const startPromises = selectedIds.map(id => _bpStartOne(id));
await Promise.all(startPromises);
```

### 3. 内存管理

```javascript
// 限制日志数量
if(window._plogs.length > 500) {
    window._plogs.length = 500;
}

// 定期清理不活跃的任务
setInterval(() => {
    Object.keys(window._pt).forEach(tid => {
        if(!window._pt[tid].running) {
            delete window._pt[tid];
        }
    });
}, 60000);
```

---

## 九、安全特性

### 1. 防误触机制

- 需要手动确认每个账号的选择
- 重要操作前显示提示
- 停止后立即生效

### 2. 数据验证

```javascript
// 验证 token 有效性
if(r.getWebSocketStatus(tid) !== "connected") {
    console.log("WebSocket 未连接", "error");
    return;
}

// 验证必要参数
if(!tid || !st) {
    throw new Error("无效的参数");
}
```

---

## 十、使用流程

1. **选择账号**：在复选框中选择要批量推图的账号
2. **配置道具**：选择是否使用火把道具（可选）
3. **启动推图**：点击"全部开始"按钮
4. **监控状态**：实时查看各账号推进进度
5. **停止操作**：需要停止时点击"全部停止"

---

## 总结

该批量推图系统具备以下特点：

✅ **高可靠性**：三重失败保护机制  
✅ **智能化**：自动 Boss 识别、智能 retry  
✅ **多开支持**：支持多账号并发执行  
✅ **人性化**：实时进度展示、详细日志记录  
✅ **容错性强**：完善的异常处理和网络恢复  
✅ **可扩展性好**：模块化设计，易于维护和升级
