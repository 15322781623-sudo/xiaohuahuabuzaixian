# 十殿阎罗挑战 - 全部实现代码

> 本文档记录游戏功能页面活动模块下"十殿阎罗挑战"的所有实现代码，包括路由、入口UI、组队页面、预设系统、实时战斗、Mock映射和UI所有代码。

---

## 1. 路由配置

**文件：** `router/index.js` (lines 76-84)

```js
{
  path: 'nightmare-battle',
  name: 'NightmareBattle',
  component: () => import('@/views/NightmareBattle.vue'),
  meta: {
    title: '十殿战斗',
    requiresToken: true
  }
}
```

---

## 2. 入口 UI + 智能导航

**文件：** `components/GameStatus.vue`

### 2.1 触发器卡片 (lines 196-218)

```html
<!-- 十殿挑战 -->
<div v-show="activeSection === 'activity'" class="nightmare-challenge-trigger">
  <MyCard class="nightmare-challenge-entry" statusClass="energy">
    <template #icon>
      <img src="/icons/ta.png" alt="十殿挑战" />
    </template>
    <template #title>
      <h3>十殿阎罗挑战</h3>
      <p>作为队长组队，完成十殿阎罗1-8关挑战。十殿混沌罗盘抽奖。</p>
    </template>
    <template #badge>
      <span>活动</span>
    </template>
    <template #default>
      <p class="description" style="color: #ff0033;">十殿阎罗挑战，作为队长开组，选择最多4个队友，完成1-8关挑战。也可以提前预设组队阵容，完成一键挑战。更多功能建设中</p>
    </template>
    <template #action>
      <button class="action-button" @click="handleOpenNightmareChallenge" :disabled="isOpeningNightmare">
        {{ isOpeningNightmare ? '检查中...' : '打开十殿挑战' }}
      </button>
    </template>
  </MyCard>
</div>
```

### 2.2 Modal 弹窗 (lines 234-246)

```html
<!-- 十殿挑战 Modal -->
<n-modal
  v-model:show="showNightmareChallengeModal"
  preset="card"
  title="十殿阎罗挑战"
  style="width: 90%; max-width: 760px"
  :bordered="true"
  :segmented="{ content: true, footer: true }"
  :closable="true"
  :mask-closable="true"
>
  <NightmareChallengeCard />
</n-modal>
```

### 2.3 导入组件 + 响应式变量

```js
import NightmareChallengeCard from "./cards/NightmareChallengeCard.vue";

const showNightmareChallengeModal = ref(false);
const isOpeningNightmare = ref(false);
```

### 2.4 智能导航 handleOpenNightmareChallenge (lines 447-532)

```js
const handleOpenNightmareChallenge = async () => {
  if (!tokenStore.selectedToken) {
    message.warning("请先选择Token");
    return;
  }
  const tokenId = tokenStore.selectedToken.id;
  const status = tokenStore.getWebSocketStatus(tokenId);
  if (status !== "connected") {
    message.warning("WebSocket未连接，请先建立连接");
    return;
  }
  isOpeningNightmare.value = true;
  try {
    // Step 1: 获取 roleId
    let roleInfo = tokenStore.gameData?.roleInfo;
    if (!roleInfo?.role?.roleId) {
      try { roleInfo = await tokenStore.sendGetRoleInfo(tokenId, {}); } catch {}
    }
    const roleId = roleInfo?.role?.roleId;
    if (!roleId) {
      message.warning("无法获取角色信息，请刷新后重试");
      isOpeningNightmare.value = false;
      return;
    }
    // Step 2: 检查 nightmare_getroleinfo → roomId（重试3次×2秒）
    let roomId = null;
    const maxRetries = 3;
    for (let attempt = 1; attempt <= maxRetries; attempt++) {
      try {
        const nightmareRoleInfo = await tokenStore.sendMessageWithPromise(
          tokenId, "nightmare_getroleinfo",
          { roleId: Number(roleId) }, 10000
        );
        roomId = nightmareRoleInfo?.nightMareData?.roomId;
        if (roomId) break;
        if (attempt < maxRetries) await new Promise(resolve => setTimeout(resolve, 2000));
      } catch (err) {
        if (attempt < maxRetries) await new Promise(resolve => setTimeout(resolve, 2000));
      }
    }
    if (roomId) {
      // 战斗中 → 直接进入战斗页面
      message.info("检测到战斗中，正在进入战斗页面...");
      router.push({
        name: "NightmareBattle",
        query: { captainTokenId: tokenId, roomId: String(roomId) },
      });
      isOpeningNightmare.value = false;
      return;
    }
    // Step 3: 非战斗中 → 打开组队 Modal
    showNightmareChallengeModal.value = true;
  } catch (err) {
    message.error(`检查进度失败: ${err.message || String(err)}`);
  } finally {
    isOpeningNightmare.value = false;
  }
};
```

---

## 3. 核心组队组件 NightmareChallengeCard.vue

**文件：** `components/cards/NightmareChallengeCard.vue` (完整 1745 行)

### 3.1 Template

```html
<template>
  <div class="nightmare-challenge-container">
    <div class="page-title">十殿阎罗挑战-组队</div>

    <!-- 初始化中 -->
    <div v-if="isInitializing" class="init-section">
      <n-spin size="small" />
      <span class="init-text">正在检查队伍状态...</span>
    </div>

    <!-- 非队长警告 -->
    <n-alert v-if="isNotLeader" type="warning" title="⚠️ 非队长账号" :bordered="true">
      <p>当前连接的账号不是十殿队伍的队长。</p>
      <p>实际队长信息：{{ actualLeaderName }}</p>
      <p>请切换到队长账号后重新打开本页面。</p>
    </n-alert>

    <template v-if="!isInitializing && !isNotLeader">
    <!-- 队长信息 + 创建房间 -->
    <div class="captain-section">
      <div class="captain-info">
        <span class="label">队长：</span>
        <span class="captain-name">{{ captainName }}</span>
      </div>
      <div class="captain-actions">
        <n-button type="primary" @click="createRoom" :loading="isCreating" :disabled="!!teamId || isCreating">
          {{ teamId ? '房间已创建' : '创建房间' }}
        </n-button>
        <n-button type="warning" @click="presetRef?.open()" style="color: #fff;">
          设置和执行一键挑战
        </n-button>
        <n-tag type="success" v-if="teamId" size="medium">TeamId: {{ teamId }}</n-tag>
      </div>
    </div>

    <!-- 队伍成员列表 -->
    <div class="team-members-section" v-if="teamMembers.length > 0">
      <div class="section-header">
        <span class="section-title">队伍成员（{{ teamMembers.length }}人）</span>
        <div class="header-actions">
          <n-tag size="small" :type="allPrepared ? 'success' : 'warning'">
            {{ allPrepared ? '全部已准备' : '等待准备' }}
          </n-tag>
          <n-button size="tiny" @click="refreshTeamMembers" :loading="isRefreshing">刷新</n-button>
        </div>
      </div>
      <div class="members-grid">
        <div v-for="(member, idx) in teamMembersWithTokens" :key="member.roleId || idx"
          class="member-card" :class="{ captain: idx === 0, prepared: member.prepared === 1 }">
          <div class="member-avatar">
            <div v-if="idx !== 0" class="member-checkbox">
              <n-checkbox :checked="selectedMemberRoleIds.includes(String(member.roleId))"
                @update:checked="(val) => toggleMemberSelect(member.roleId, val)" size="small" />
            </div>
            <img v-if="member.headImg" :src="member.headImg" :alt="member.name" class="avatar-img"
              @error="($event.target.style.display = 'none')" />
            <div v-else class="avatar-placeholder">{{ (member.name || '?')[0] }}</div>
            <div class="member-prepared-badge" v-if="member.prepared === 1">✅</div>
          </div>
          <div class="member-info">
            <div class="member-name">
              {{ member.name }}
              <n-tag v-if="idx === 0" size="tiny" type="info">队长</n-tag>
              <n-tag v-if="member.matchedTokenId" size="tiny" type="success">已匹配</n-tag>
            </div>
            <div class="member-meta">
              <span class="meta-item">区服: {{ member.serverId }}</span>
              <span class="meta-item">战力: {{ formatPower(member.power) }}</span>
            </div>
            <div class="member-meta" v-if="member.legionName">
              <span class="meta-item">军团: {{ member.legionName }}</span>
            </div>
            <div class="member-status-row">
              <span class="status-label">枕头:</span>
              <span :class="member.pillowCnt === 0 ? 'pillow-zero' : 'pillow-normal'">{{ member.pillowCnt }}</span>
              <span v-if="member.pillowCnt === 0" class="pillow-warning">枕头不足</span>
              <span v-if="idx !== 0" class="status-label" style="margin-left:8px">准备:</span>
              <n-tag v-if="idx !== 0" size="tiny" :type="member.prepared === 1 ? 'success' : 'error'">
                {{ member.prepared === 1 ? '已准备' : '未准备' }}
              </n-tag>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- 队友选择区域 -->
    <div class="teammate-section" v-if="teamId">
      <div class="section-header">
        <span class="section-title">选择队友（最多4人）</span>
        <span class="section-hint">仅显示同大区账号</span>
      </div>
      <div class="teammate-select">
        <n-select v-model:value="selectedTeammates" :options="teammateOptions" multiple
          placeholder="请选择队友（最多4人）" :max-tag-count="4" :maxlength="4" filterable clearable
          @update:value="handleTeammateChange" />
      </div>
    </div>

    <!-- 操作按钮区 -->
    <div class="action-section" v-if="teamId">
      <n-button type="primary" @click="joinAndReady" :loading="isJoiningAndReady"
        :disabled="selectedTeammates.length === 0 || isJoining || isJoiningAndReady">
        加入房间并准备
      </n-button>
      <n-button type="warning" @click="prepareSelected" :loading="isPreparing"
        :disabled="selectedMemberRoleIds.length === 0 || isPreparing">
        准备选中队员 ({{ selectedMemberRoleIds.length }})
      </n-button>
      <n-button type="success" @click="startBattle" :loading="isStarting" :disabled="isStarting">
        开始战斗
      </n-button>
      <n-button type="error" @click="dismissRoom" :loading="isDismissing" :disabled="isDismissing">
        解散
      </n-button>
    </div>

    <!-- 战斗中状态 -->
    <div class="fighting-section" v-if="pageState === 'fighting'">
      <n-alert type="success" title="战斗中">
        十殿阎罗挑战进行中，TeamId: {{ teamId }}
      </n-alert>
    </div>

    <!-- 操作日志区 -->
    <div class="log-section">
      <div class="log-header">
        <span class="log-title">操作日志</span>
        <n-button text size="tiny" @click="clearLogs">清空</n-button>
      </div>
      <div class="log-container" ref="logContainerRef">
        <div v-for="(log, idx) in operationLogs" :key="idx" class="log-entry" :class="log.type">
          <span class="log-time">{{ log.time }}</span>
          <span class="log-msg">{{ log.msg }}</span>
        </div>
        <div v-if="operationLogs.length === 0" class="log-empty">暂无操作日志</div>
      </div>
    </div>
    </template>

    <!-- 一键挑战预设组件 -->
    <NightmarePreset ref="presetRef" :captain-token-id="captainTokenId"
      :member-token-ids="presetMemberTokenIds" @execute="onPresetExecute" />
  </div>
</template>
```

### 3.2 Script

```js
<script setup>
import { ref, computed, nextTick, watch, onMounted } from "vue";
import { useRouter } from "vue-router";
import { useTokenStore } from "@/stores/tokenStore";
import { useMessage } from "naive-ui";
import NightmarePreset from "./NightmarePreset.vue";

const tokenStore = useTokenStore();
const router = useRouter();
const message = useMessage();
const presetRef = ref(null);

// ====== 页面状态 ======
const pageState = ref("teamBuilding");
const isInitializing = ref(true);
const isNotLeader = ref(false);
const actualLeaderName = ref("");

// ====== 队长信息 ======
const captainTokenId = ref("");
const captainRoleId = ref("");

const initCaptainTokenId = () => {
  const current = tokenStore.selectedTokenId || "";
  if (current && current !== captainTokenId.value) {
    captainTokenId.value = current;
    addLog(`队长账号已设为: ${current.slice(0, 8)}`);
  }
};

watch(() => tokenStore.selectedTokenId, (newVal) => {
  if (!captainTokenId.value && newVal) {
    captainTokenId.value = newVal;
    addLog(`队长账号初始化: ${newVal.slice(0, 8)}`);
  }
});

const captainTokenObj = computed(() => tokenStore.gameTokens.find((t) => t.id === captainTokenId.value) || null);
const captainName = computed(() => {
  if (!captainTokenObj.value) return "未选择账号";
  const t = captainTokenObj.value;
  return `${t.name} (${t.server || "未知区服"})`;
});
const captainServer = computed(() => captainTokenObj.value?.server || "");

// 按500服为一个大区划分
const getZoneRange = (server) => {
  if (!server) return "";
  const s = parseInt(String(server), 10);
  if (isNaN(s)) return "";
  const start = Math.floor((s - 1) / 500) * 500 + 1;
  const end = start + 499;
  return `${start}~${end}`;
};
const captainZone = computed(() => getZoneRange(captainServer.value));

const formatPower = (power) => {
  if (!power && power !== 0) return "未知";
  const p = Number(power);
  if (p >= 1e9) return (p / 1e8).toFixed(1) + "亿";
  if (p >= 1e8) return (p / 1e8).toFixed(2) + "亿";
  if (p >= 1e4) return (p / 1e4).toFixed(1) + "万";
  return p.toLocaleString();
};

const allPrepared = computed(() => {
  if (teamMembers.value.length <= 1) return false;
  return teamMembers.value.slice(1).every((m) => m.prepared === 1);
});

// ====== 房间 & 队伍 ======
const teamId = ref("");
const isCreating = ref(false);
const teamMembers = ref([]);

const normalizeName = (name) => {
  if (!name) return "";
  return String(name)
    .replace(/[\u00B9\u00B2\u00B3\u2070-\u2079]/g, "")
    .replace(/[\u272A-\u272F\u2730-\u2736]/g, "")
    .replace(/[^\u4e00-\u9fa5a-zA-Z0-9]/g, "").toLowerCase();
};

const teamMembersWithTokens = computed(() => {
  return teamMembers.value.map((member) => {
    const memberRoleId = String(member.roleId || "");
    const memberServerId = String(member.serverId || "");
    const memberNormName = normalizeName(member.name);
    let matchedToken = null;
    if (memberRoleId) {
      matchedToken = tokenStore.gameTokens.find((t) => t.roleId && String(t.roleId) === memberRoleId);
    }
    if (!matchedToken && memberNormName) {
      matchedToken = tokenStore.gameTokens.find((t) => {
        const tokenServer = String(t.server || "");
        return normalizeName(t.name) === memberNormName && tokenServer === memberServerId;
      });
    }
    if (!matchedToken && memberNormName) {
      matchedToken = tokenStore.gameTokens.find((t) => normalizeName(t.name) === memberNormName);
    }
    const pillowCnt = member.extParam?.itemCnt != null ? Number(member.extParam.itemCnt) : 5;
    return { ...member, matchedTokenId: matchedToken?.id || null, pillowCnt };
  });
});

// ====== 队友选择 ======
const selectedTeammates = ref([]);
const presetMemberTokenIds = computed(() => {
  if (teamMembersWithTokens.value.length > 1) {
    return teamMembersWithTokens.value.slice(1).map((m) => m.matchedTokenId).filter(Boolean);
  }
  return selectedTeammates.value;
});

const handleTeammateChange = (value) => {
  if (value.length > 4) { message.warning("最多选择4名队友"); selectedTeammates.value = value.slice(0, 4); }
  else { selectedTeammates.value = value; }
};

const selectedMemberRoleIds = ref([]);
const toggleMemberSelect = (roleId, checked) => {
  const rid = String(roleId);
  if (checked) { if (!selectedMemberRoleIds.value.includes(rid)) selectedMemberRoleIds.value.push(rid); }
  else { const idx = selectedMemberRoleIds.value.indexOf(rid); if (idx !== -1) selectedMemberRoleIds.value.splice(idx, 1); }
};
const isPreparing = ref(false);

const teammateOptions = computed(() => {
  return tokenStore.gameTokens
    .filter((t) => { if (t.id === captainTokenId.value) return false; return getZoneRange(t.server || "") === captainZone.value; })
    .sort((a, b) => parseInt(a.server || "0", 10) - parseInt(b.server || "0", 10))
    .map((t) => ({ label: `${t.name} (${t.server}) [${t.id.slice(0, 8)}]`, value: t.id }));
});

// ====== 操作日志 ======
const operationLogs = ref([]);
const logContainerRef = ref(null);
const addLog = (msg, type = "info") => {
  const now = new Date();
  operationLogs.value.push({ time: now.toLocaleTimeString("zh-CN", { hour12: false }), msg, type });
  nextTick(() => { if (logContainerRef.value) logContainerRef.value.scrollTop = logContainerRef.value.scrollHeight; });
};
const clearLogs = () => { operationLogs.value = []; };

// ====== 连接管理 ======
const delay = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

const ensureCaptainConnected = async () => {
  const status = tokenStore.getWebSocketStatus(captainTokenId.value);
  if (status === "connected") return true;
  addLog("队长未连接，正在连接...");
  tokenStore.selectToken(captainTokenId.value);
  let retries = 0;
  while (tokenStore.getWebSocketStatus(captainTokenId.value) !== "connected" && retries < 30) { await delay(1000); retries++; }
  if (tokenStore.getWebSocketStatus(captainTokenId.value) !== "connected") { addLog("队长连接超时", "error"); return false; }
  addLog("队长连接成功", "success");
  return true;
};

const connectAndDo = async (tid, name, actionFn) => {
  const token = tokenStore.gameTokens.find((t) => t.id === tid);
  if (!token) { addLog(`[${name}] Token未找到`, "error"); return false; }
  const savedSelectedId = tokenStore.selectedTokenId;
  let status = tokenStore.getWebSocketStatus(tid);
  if (status !== "connected") {
    addLog(`[${name}] 正在连接...`);
    tokenStore.createWebSocketConnection(tid, token.token, token.wsUrl || null);
    let retries = 0;
    while (tokenStore.getWebSocketStatus(tid) !== "connected" && retries < 30) { await delay(1000); retries++; }
    if (tokenStore.getWebSocketStatus(tid) !== "connected") {
      addLog(`[${name}] 连接超时`, "error");
      if (tokenStore.selectedTokenId !== savedSelectedId) tokenStore.selectedTokenId = savedSelectedId;
      return false;
    }
    // 获取并保存 roleId
    if (token && !token.roleId) {
      try {
        const roleInfo = await tokenStore.sendMessageWithPromise(tid, "role_getroleinfo", {}, 10000);
        const rid = roleInfo?.role?.roleId;
        if (rid) {
          const idx = tokenStore.gameTokens.findIndex((t) => t.id === tid);
          if (idx !== -1) {
            tokenStore.gameTokens = tokenStore.gameTokens.map((t, i) => i === idx ? { ...t, roleId: String(rid) } : t);
          }
        }
      } catch {}
    }
  }
  try { await actionFn(tid); return true; }
  catch (err) { addLog(`[${name}] 操作失败: ${err.message || err}`, "error"); return false; }
  finally { if (tokenStore.selectedTokenId !== savedSelectedId) tokenStore.selectedTokenId = savedSelectedId; }
};

const disconnectTeammate = async (tid) => {
  try { await tokenStore.closeWebSocketConnection(tid); } catch {}
};
```

### 3.3 Script（续）— 初始化 + 核心操作

```js
// ====== 初始化：检查已有队伍 ======
onMounted(async () => {
  initCaptainTokenId();
  const connected = await ensureCaptainConnected();
  if (!connected) { isInitializing.value = false; return; }

  let roleInfo = tokenStore.gameData.roleInfo;
  if (!roleInfo?.role?.roleId) roleInfo = await tokenStore.sendGetRoleInfo(captainTokenId.value, {});
  const roleId = roleInfo?.role?.roleId;
  if (!roleId) { isInitializing.value = false; return; }
  captainRoleId.value = String(roleId);

  // 检查已有队伍
  const roleTeamRes = await tokenStore.sendMessageWithPromise(
    captainTokenId.value, "matchteam_getroleteaminfo", { roleID: roleId }, 10000);
  const gDMTData = roleTeamRes?.roleMTData?.gDMTData || {};
  const numericTeamKeys = Object.keys(gDMTData).filter((k) => /^\d+$/.test(k));
  if (numericTeamKeys.length === 0) { isInitializing.value = false; return; }

  const existingTeamId = gDMTData[numericTeamKeys[0]]?.teamId;
  if (!existingTeamId) { isInitializing.value = false; return; }

  // 获取队伍详情 & 验证队长身份
  const teamInfoRes = await tokenStore.sendMessageWithPromise(
    captainTokenId.value, "matchteam_getteaminfo", { teamId: existingTeamId }, 10000);
  const teamInfo = teamInfoRes.teamInfo;
  if (String(teamInfo.leaderId) !== String(roleId)) {
    isNotLeader.value = true;
    isInitializing.value = false;
    return;
  }

  // 恢复队伍状态
  teamId.value = String(existingTeamId);
  teamMembers.value = teamInfo.fightRoleBase || [];
  isInitializing.value = false;
});

// ====== 创建房间 ======
const createRoom = async () => {
  const createResp = await tokenStore.sendMessageWithPromise(captainTokenId.value, "matchteam_create", {
    teamCfgId: 1, setting: { name: "十殿先锋队", notice: "", secret: 1, apply: 0, applyList: [] },
    param: 0, custom: {}, extParam: 0,
  }, 10000);
  teamId.value = String(createResp.teamInfo.teamId);
};

// ====== 加入房间并准备 ======
const joinAndReady = async () => {
  for (const tid of selectedTeammates.value) {
    await connectAndDo(tid, name, async (tokenId) => {
      await tokenStore.sendMessageWithPromise(tokenId, "matchteam_getrandteamlist", { teamCfgId: 1, param: 0, custom: {} }, 10000);
      await tokenStore.sendMessageWithPromise(tokenId, "matchteam_join", { teamId: Number(teamId.value) }, 10000);
      await delay(1000);
      await tokenStore.sendMessageWithPromise(tokenId, "matchteam_memberprepare", { teamId: Number(teamId.value) }, 10000);
    });
    await disconnectTeammate(tid);
    await delay(500);
  }
  await refreshTeamMembers();
};

// ====== 准备选中队员 ======
const prepareSelected = async () => {
  for (const item of selectedTokens) {
    await connectAndDo(item.tokenId, item.name, async (tokenId) => {
      await tokenStore.sendMessageWithPromise(tokenId, "matchteam_memberprepare", { teamId: Number(teamId.value) }, 10000);
    });
    await disconnectTeammate(item.tokenId);
    await delay(300);
  }
  await refreshTeamMembers();
};

// ====== 开始战斗 ======
const startBattle = async (presetId = null) => {
  await tokenStore.sendMessageWithPromise(captainTokenId.value, "matchteam_openteam", { teamId: Number(teamId.value) }, 10000);
  // 获取 roomId（重试10次×3秒）
  let roomId = null;
  for (let attempt = 1; attempt <= 10; attempt++) {
    const resp = await tokenStore.sendMessageWithPromise(captainTokenId.value, "nightmare_getroleinfo",
      { roleId: Number(captainRoleId.value) }, 10000);
    roomId = resp?.nightMareData?.roomId;
    if (roomId) break;
    await delay(3000);
  }
  router.push({
    name: "NightmareBattle",
    query: { teamId: teamId.value, captainTokenId: captainTokenId.value,
      ...(roomId ? { roomId: String(roomId) } : {}), ...(presetId ? { presetId } : {}) },
  });
};

// ====== 解散房间 ======
const dismissRoom = async () => {
  await tokenStore.sendMessageWithPromise(captainTokenId.value, "matchteam_dismiss", { teamId: Number(teamId.value) }, 10000);
  teamId.value = ""; teamMembers.value = []; selectedTeammates.value = [];
};

// ====== 预设执行：一键挑战 ======
const onPresetExecute = async (preset) => {
  presetRef.value?.close();
  captainTokenId.value = preset.captainTokenId;
  selectedTeammates.value = [...(preset.memberTokenIds || [])].slice(0, 4);
  if (!teamId.value) { await createRoom(); await delay(1500); }
  if (selectedTeammates.value.length > 0) await joinAndReady();
  await startBattle(preset.id);
};
</script>
```

---

## 4. 一键挑战预设 NightmarePreset.vue

**文件：** `components/cards/NightmarePreset.vue` (完整 655 行)

### 4.1 Boss 名称映射

```js
const BOSS_NAME = {
  1: "秦广王", 2: "楚江王", 3: "宋帝王", 4: "五官王",
  5: "阎罗王", 6: "卞城王", 7: "泰山王", 8: "都市王",
};
```

### 4.2 预设数据结构 (localStorage key: `nightmare-presets`)

```js
{
  id: "nm_preset_" + timestamp,
  name: "预设名称",
  captainTokenId: "队长TokenID",
  memberTokenIds: ["队员1TokenID", ...],
  levelConfig: {
    1: { priority: ["roleId1", ...], recovery: { "roleId1": true, ... } },
    // ... 2-8关
  },
  createdAt: timestamp, updatedAt: timestamp,
}
```

### 4.3 Template 结构

- 预设列表弹窗：新建/编辑/删除/一键挑战
- 编辑弹窗：名称 + 成员标签 + 8关优先级卡片（点击交换） + 恢复 checkbox + 套用第1关
- Props: `captainTokenId`, `memberTokenIds`; Emits: `execute`
- defineExpose: `{ open, close }`

### 4.4 Script 核心逻辑

```js
// 初始化关卡配置：自动从 memberTokenIds + captainTokenId 提取 roleId
const initLevelConfig = (lv) => {
  const priority = [];
  for (const tid of form.memberTokenIds) {
    const token = tokenStore.gameTokens.find(t => t.id === tid);
    if (token?.roleId) priority.push(String(token.roleId));
  }
  if (form.captainTokenId) {
    const token = tokenStore.gameTokens.find(t => t.id === form.captainTokenId);
    if (token?.roleId) priority.push(String(token.roleId));
  }
  form.levelConfig[lv] = { priority, recovery: {} };
};

// 点击卡片交换（支持同关和跨关）
const handleCardClick = (lv, roleId, idx) => {
  if (!swapSource.value) { swapSource.value = { lv, roleId, idx }; return; }
  if (swapSource.value.lv === lv && swapSource.value.roleId === roleId) { swapSource.value = null; return; }
  // 同关：交换数组位置；跨关：替换对方位置的 roleId
  swapSource.value = null;
};

const applyLv1ToAll = () => { /* 深拷贝第1关配置到2-8关 */ };

// CRUD: localStorage("nightmare-presets") 读写
// open/close 对外暴露
```

---

## 5. 实时战斗页面 NightmareBattle.vue

**文件：** `views/NightmareBattle.vue` (完整 1513 行)

### 5.1 路由参数

```js
const teamId = computed(() => route.query.teamId || mockBattleData.teamId);
const captainTokenId = ref(route.query.captainTokenId || "");
const isTest = computed(() => route.query.test === "true");
const roomId = ref(route.query.roomId || "");
const presetId = ref(route.query.presetId || "");
const presetData = ref(null);
const isAutoMode = computed(() => !!presetId.value && !!presetData.value);
```

### 5.2 战斗流程状态

```js
const battlePhase = ref("idle");      // 'idle' | 'fighting' | 'cooling'
const battleCountdown = ref(0);
const attackRecords = ref({});        // { [level]: [roleId1, roleId2] }
// 战斗流程：10秒战斗 → leadercomplete → getroominfo → 18秒冷却
```

### 5.3 怪物 HP 百分比 / 血量格式化

```js
// ====== 工具函数 ======
const formatNumber = (num) => {
  if (num == null) return "0";
  return Number(num).toLocaleString();
};

const getMonsterHpPercent = (monster) => {
  if (!monster || !monster.maxHp || monster.maxHp === 0) return 0;
  return (Math.max(0, monster.currentHp) / monster.maxHp) * 100;
};

const getHpClass = (percent) => {
  if (percent > 60) return "hp-high";
  if (percent > 30) return "hp-mid";
  return "hp-low";
};

// ====== 怪物怒气百分比 ======
const getMonsterEnergyPercent = (monster) => {
  if (!monster || !monster.maxEnergy || monster.maxEnergy === 0) return 0;
  return (monster.currentEnergy / monster.maxEnergy) * 100;
};
```

### 5.4 Boss/怪物最大HP参考数据（各关卡固定满血量）

```js
const BOSS_MAX_HP = {
  1: { boss: 225300000000, minion: 75120000000 },   // 秦广王 2253亿, 小怪 751.2亿
  2: { boss: 247900000000, minion: 82640000000 },   // 楚江王 2479亿, 小怪 826.4亿
  3: { boss: 272700000000, minion: 0 },              // 宋帝王 2727亿
  4: { boss: 299900000000, minion: 0 },              // 五官王 2999亿
  5: { boss: 329900000000, minion: 300500000000 },   // 阎罗王 3299亿, 小怪 3005亿
  6: { boss: 300, minion: 0 },                       // 卞城王 300（实际血量）
  7: { boss: 751200000000, minion: 0 },              // 泰山王 7512亿
  8: { boss: 788800000000, minion: 0 },              // 都市王 7888亿
};
```

### 5.5 怪物血量格式化（第6关用实际数值，其他关用亿缩写）

```js
const formatMonsterHp = (hp, level) => {
  if (hp == null) return "0";
  const num = Number(hp);
  if (Number(level) === 6) return num.toLocaleString();
  const yi = num / 100000000;
  if (yi >= 100) return yi.toFixed(0) + "亿";
  return yi.toFixed(1) + "亿";
};
```

### 5.6 parseRoomInfo 核心解析（完整代码）

```js
const parseRoomInfo = (roomInfo, captainRoleId) => {
  if (!roomInfo) {
    addLog("roomInfo 为空，无法解析", "error");
    return false;
  }

  // 1. 解析关卡信息
  currentLevel.value = roomInfo.curMonsterCfgId || 0;
  lastMonsterId.value = roomInfo.lastMonsterId || 0;

  // 2. 解析上一轮战斗结果
  lastFighter.value = roomInfo.lastFighter || null;
  if (roomInfo.lastBattleData) {
    lastBattleResult.value = roomInfo.lastBattleData.result || { isWin: null };
  }
  if (lastFighter.value) {
    const frb = roomInfo.fightRoleBase || {};
    lastFighterName.value = Array.isArray(frb)
      ? (frb.find((m) => String(m.roleId) === String(lastFighter.value))?.name || String(lastFighter.value))
      : (frb[lastFighter.value]?.name || String(lastFighter.value));
  }

  // 3. 解析怪物/Boss 信息（当前关卡的 monsterTeamInfo[level].monsterTeam.team）
  const fightRoleBase = roomInfo.fightRoleBase || {};
  const playerTeamInfo = roomInfo.playerTeamInfo || {};
  const monsterTeamInfo = roomInfo.monsterTeamInfo || {};
  const curLevel = String(currentLevel.value);
  const levelMonsterInfo = monsterTeamInfo[curLevel];
  const monsterTeam = levelMonsterInfo?.monsterTeam?.team || {};

  if (Object.keys(monsterTeam).length === 0) {
    addLog("当前关卡尚未开打，等待战斗数据...", "warning");
    monsters.value = [];
  } else {
    // 获取参考满血量（各关卡固定值，服务器 hp 字段可能为0）
    const refData = BOSS_MAX_HP[Number(curLevel)] || { boss: 0, minion: 0 };
    // curMonsterData 在出战后才有值（curHp/curEnergy），战斗前为空对象
    const curMonsterData = levelMonsterInfo?.curMonsterData || {};

    const parsedMonsters = [];
    for (const [slotKey, data] of Object.entries(monsterTeam)) {
      // Boss判断：id长度<=6位为Boss，否则为喽啰
      const isBoss = String(data.id).length <= 6;
      const monsterId = String(data.id);

      // 从 curMonsterData 获取当前HP/怒气（出战后才有）
      const cd = curMonsterData[monsterId] || {};

      // 使用参考表的最大HP（服务器返回的 hp 可能为0）
      const refMaxHp = isBoss ? refData.boss : refData.minion;
      const maxHp = refMaxHp > 0 ? refMaxHp : (data.hp || 0);
      // curHp: curMonsterData 有值则用它，否则用 maxHp（显示100%）
      const curHp = cd.curHp != null ? cd.curHp : maxHp;
      const curEnergy = cd.curEnergy != null ? cd.curEnergy : 0;

      parsedMonsters.push({
        id: data.id,
        name: getMonsterName(data.id),
        maxHp,
        currentHp: curHp,
        maxEnergy: 100,
        currentEnergy: curEnergy,
        isBoss,
      });
    }
    monsters.value = parsedMonsters;
  }

  // 4. 解析战斗成员（fightRoleBase → 成员 + 武将初始HP，playerTeamInfo → 当前HP/Energy）
  //    兼容 fightRoleBase 的两种格式：数组 [{roleId, name, ...}] 和对象 {roleId: {name, ...}}
  const parsedMembers = [];
  const frbEntries = Array.isArray(fightRoleBase)
    ? fightRoleBase.map((m) => [String(m.roleId), m])
    : Object.entries(fightRoleBase);

  for (const [roleId, roleData] of frbEntries) {
    const team = roleData.battleData?.team || {};
    const curHeroData = playerTeamInfo[roleId]?.curHeroData || {};

    // 解析武将（每个成员最多5个武将）
    const heroes = [];
    for (let i = 0; i < 5; i++) {
      const heroKey = String(i);
      const heroData = team[heroKey];
      if (heroData && heroData.id) {
        const heroIdStr = String(heroData.id);
        const curData = curHeroData[heroIdStr] || curHeroData[heroData.id] || {};
        // HP% = playerTeamInfo.curHp / fightRoleBase.hp（初始满血）
        const fullHp = heroData.hp || 0;
        const curHp = curData.curHp != null ? curData.curHp : fullHp;
        // 抓包中未出战成员 curEnergy=-1，实际显示为 0
        const curEnergy = curData.curEnergy != null && curData.curEnergy >= 0 ? curData.curEnergy : 0;
        heroes.push({
          id: heroData.id,
          name: getHeroName(heroData.id),
          maxHp: fullHp,
          currentHp: curHp,
          maxEnergy: 100,
          currentEnergy: curEnergy,
          isAlive: curHp > 0,
          type: heroData.type,
          recordFlag: heroData.recordFlag,
        });
      }
    }

    // 判断该成员是否所有武将都已死亡（HP全部为0）
    const isAllHeroesDead = heroes.length > 0 && heroes.every(h => !h.isAlive);

    parsedMembers.push({
      roleId: String(roleId),
      name: roleData.name || String(roleId),
      heroes,
      isCaptain: String(roleId) === String(captainRoleId),
      isAllHeroesDead,
      hasFought: false,
    });
  }

  members.value = parsedMembers;
  return true;
};
```

### 5.7 战斗指令流程

```
startAttack(member):
  1. nightmare_fight → recordMemberFought
  2. 10秒战斗倒计时
  3. nightmare_leadercomplete（重试3次×5秒）
  4. nightmare_getroominfo 刷新数据
  5. 通关：第8关完成后响应为空 → 返回组队页
  6. startCooling → 18秒冷却

memberRecover(member):
  nightmare_restore → nightmare_getroominfo 刷新
```

### 5.8 自动出战（一键挑战）

```js
getAutoAttacker() → 按预设优先级找本关未出战的第一个成员
autoAttack() → 检查恢复设置 → memberRecover → startAttack
watch(battlePhase) → idle + isAutoMode → 延迟1秒自动出战
onMounted → 3秒后首轮自动出战
```

### 5.9 测试模式

```js
// 使用 nightmareBattleMockData.json 的 fightSnapshots 模拟出战
// fightSnapshotIndex 递增，每次出战用下一个快照的 roomInfo 刷新 UI
```

### 5.10 Template 结构（完整原始代码，见源文件 lines 1-202）

```html
<template>
  <div class="nightmare-battle-container">
    <!-- 页面标题 + 刷新按钮 + 倒计时 -->
    <div class="battle-header">
      <div class="header-left">
        <h2 class="page-title">十殿阎罗挑战 - 战斗中</h2>
        <n-tag type="info" size="medium">RoomId: {{ roomId }}</n-tag>
        <n-tag v-if="isAutoMode" type="success" size="small">🤖 一键挑战模式</n-tag>
      </div>
      <div class="header-right">
        <div v-if="battlePhase !== 'idle'" class="battle-phase-status">
          <n-tag :type="battlePhase === 'fighting' ? 'warning' : 'info'" size="medium">
            {{ battleCountdownLabel }} {{ battleCountdown }}s
          </n-tag>
        </div>
        <n-button size="small" @click="refreshBattle" :loading="isRefreshing">刷新</n-button>
        <n-button size="small" type="error" @click="goBack" secondary :disabled="battlePhase !== 'idle'">返回组队</n-button>
      </div>
    </div>

    <!-- Boss 信息区域 -->
    <div class="boss-section">
      <div class="section-label">
        第{{ currentLevel }}关 · {{ bossName }}
        <n-tag size="small" :type="currentLevel <= 3 ? 'info' : currentLevel <= 6 ? 'warning' : 'error'">
          关卡 {{ currentLevel }}/{{ totalLevels }}
        </n-tag>
      </div>

      <!-- 怪物列表 -->
      <div class="monsters-grid">
        <div v-for="monster in monsters" :key="monster.id"
          class="monster-card" :class="{ boss: monster.isBoss, dead: monster.currentHp <= 0 }">
          <div class="monster-header">
            <span class="monster-name">
              {{ monster.name }}
              <n-tag v-if="monster.isBoss" size="tiny" type="error">BOSS</n-tag>
            </span>
            <span class="monster-hp-text">
              {{ formatMonsterHp(monster.currentHp, currentLevel) }} / {{ formatMonsterHp(monster.maxHp, currentLevel) }}
            </span>
          </div>
          <div class="hp-bar-wrap">
            <div class="hp-bar-fill" :class="getHpClass(getMonsterHpPercent(monster))"
              :style="{ width: getMonsterHpPercent(monster) + '%' }"></div>
            <span class="hp-bar-percent">{{ getMonsterHpPercent(monster).toFixed(0) }}%</span>
          </div>
          <!-- 怒气条 -->
          <div class="monster-energy-row">
            <span class="energy-label">怒气</span>
            <div class="bar-wrap energy-bar-wrap">
              <div class="bar-fill energy-fill"
                :style="{ width: getMonsterEnergyPercent(monster) + '%' }"></div>
              <span class="bar-text energy-text"
                :style="{ color: getMonsterEnergyPercent(monster) > 50 ? '#fff' : '#333' }">
                {{ monster.currentEnergy }}/{{ monster.maxEnergy }}
              </span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- 战斗成员区域 -->
    <div class="members-section">
      <div class="section-label">战斗成员（{{ members.length }}人）</div>
      <div class="members-grid">
        <div v-for="member in members" :key="member.roleId"
          class="member-card" :class="{ leader: member.isLeader }">
          <div class="member-header">
            <div class="member-row-actions">
              <div class="member-tags">
                <n-tag v-if="member.isLeader" size="tiny" type="info">队长</n-tag>
                <n-tag v-if="isMemberFoughtInCurrentLevel(member)" size="tiny" type="warning">已出战</n-tag>
                <n-tag v-if="member.isAllHeroesDead" size="tiny" type="error">不可出战</n-tag>
              </div>
              <div class="member-actions">
                <n-button size="tiny" type="success" @click="startAttack(member)"
                  :disabled="battlePhase !== 'idle' || isMemberFoughtInCurrentLevel(member) || member.isAllHeroesDead">出战</n-button>
                <n-button size="tiny" type="warning" @click="memberRecover(member)">恢复</n-button>
              </div>
            </div>
            <div class="member-name-row">
              <span class="member-name" :title="member.name">{{ member.name }}</span>
            </div>
          </div>
          <!-- 武将列表 -->
          <div class="heroes-list">
            <div v-for="hero in member.heroes" :key="hero.id"
              class="hero-item" :class="{ dead: !hero.isAlive }">
              <div class="hero-name" :title="hero.name">
                {{ hero.name.length > 4 ? hero.name.slice(0, 4) + '..' : hero.name }}
              </div>
              <div class="hero-bars">
                <div class="hero-bar-row">
                  <div class="bar-wrap">
                    <div class="bar-fill hp-fill"
                      :class="getHpClass(hero.currentHp / hero.maxHp * 100)"
                      :style="{ width: (hero.currentHp / hero.maxHp * 100) + '%' }"></div>
                    <span class="bar-text">{{ (hero.currentHp / hero.maxHp * 100).toFixed(0) }}%</span>
                  </div>
                </div>
                <div class="hero-bar-row">
                  <div class="bar-wrap">
                    <div class="bar-fill energy-fill"
                      :style="{ width: (hero.currentEnergy / hero.maxEnergy * 100) + '%' }"></div>
                    <span class="bar-text energy-text"
                      :style="{ color: (hero.currentEnergy / hero.maxEnergy * 100) > 50 ? '#fff' : '#333' }">
                      {{ hero.currentEnergy }}/{{ hero.maxEnergy }}
                    </span>
                  </div>
                </div>
              </div>
            </div>
            <!-- 空武将占位（保证5个槽位） -->
            <div v-for="n in Math.max(0, 5 - (member.heroes ? member.heroes.length : 0))" :key="'empty-' + n" class="hero-item empty">
              <div class="hero-name">空</div>
              <div class="hero-bars">
                <div class="hero-bar-row"><div class="bar-wrap empty-bar"></div></div>
                <div class="hero-bar-row"><div class="bar-wrap empty-bar"></div></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- 操作日志区域 -->
    <div class="log-section">
      <div class="log-header">
        <span class="log-title">操作日志</span>
        <n-button text size="tiny" @click="clearLogs">清空</n-button>
      </div>
      <div class="log-container" ref="logContainerRef">
        <div v-for="(log, idx) in battleLogs" :key="idx" class="log-entry" :class="log.type">
          <span class="log-time">{{ log.time }}</span>
          <span class="log-msg">{{ log.msg }}</span>
        </div>
        <div v-if="battleLogs.length === 0" class="log-empty">暂无操作日志</div>
      </div>
    </div>
  </div>
</template>
```

---

## 6. Mock 数据映射

**文件：** `utils/nightmareBattleMock.js` (179 行)

```js
import captureData from "./nightmareBattleMockData.json";

// ====== Boss ID → 名称字典（十殿阎罗 1~8 关） ======
export const bossDict = {
  1: { id: 1, name: "秦广王" },
  2: { id: 2, name: "楚江王" },
  3: { id: 3, name: "宋帝王" },
  4: { id: 4, name: "五官王" },
  5: { id: 5, name: "阎罗王" },
  6: { id: 6, name: "卞城王" },
  7: { id: 7, name: "泰山王" },
  8: { id: 8, name: "都市王" },
};

export function getBossName(level) {
  const boss = bossDict[level];
  return boss ? boss.name : `第${level}关Boss`;
}

// ====== 武将 ID → 名称字典 ======
const heroNameDict = {
  107: "吕布", 108: "赵云", 109: "关羽", 110: "诸葛亮", 111: "曹操",
  112: "貂蝉", 113: "周瑜", 114: "司马懿", 115: "张飞", 116: "黄月英",
  117: "孙尚香", 118: "大乔", 119: "小乔", 120: "孙策", 121: "孙权",
  122: "刘备", 123: "太史慈", 124: "甘宁", 125: "吕蒙", 126: "陆逊",
  127: "郭嘉", 128: "贾诩", 129: "许褚", 130: "典韦", 131: "夏侯惇",
  140: "华佗", 141: "左慈", 142: "于吉", 143: "南华老仙",
};

export function getHeroName(heroId) {
  const id = Number(heroId);
  if (captureHeroNames[id]) return captureHeroNames[id];
  return heroNameDict[id] || `武将#${heroId}`;
}

// ====== 动态构建武将名称（从抓包数据） ======
const captureHeroNames = {};

function buildHeroNamesFromCapture() {
  if (Object.keys(captureHeroNames).length > 0) return;
  const allRoomInfos = [];
  if (captureData.initialRoomInfo) allRoomInfos.push(captureData.initialRoomInfo);
  if (captureData.fightSnapshots) {
    captureData.fightSnapshots.forEach(s => {
      if (s.roomInfo) allRoomInfos.push(s.roomInfo);
    });
  }
  for (const ri of allRoomInfos) {
    const frb = ri.fightRoleBase || [];
    if (Array.isArray(frb)) {
      for (const member of frb) {
        const team = member.battleData?.team || {};
        for (const key of Object.keys(team)) {
          const hero = team[key];
          if (hero && hero.id && !captureHeroNames[hero.id]) {
            captureHeroNames[hero.id] = `武将${hero.id}`;
          }
        }
      }
    }
  }
}
buildHeroNamesFromCapture();

// ====== 怪物 ID → 名称字典（动态构建） ======
const monsterNameDict = {};

function buildMonsterNamesFromCapture() {
  const allRoomInfos = [];
  if (captureData.initialRoomInfo) allRoomInfos.push(captureData.initialRoomInfo);
  if (captureData.fightSnapshots) {
    captureData.fightSnapshots.forEach(s => {
      if (s.roomInfo) allRoomInfos.push(s.roomInfo);
    });
  }
  for (const ri of allRoomInfos) {
    const mti = ri.monsterTeamInfo || {};
    for (const [level, info] of Object.entries(mti)) {
      const team = info.monsterTeam?.team || {};
      for (const [slotKey, monster] of Object.entries(team)) {
        const id = monster.id;
        if (id && !monsterNameDict[id]) {
          // 根据关卡编号生成名称
          const bossName = bossDict[Number(level)]?.name || `第${level}关Boss`;
          // 第一个怪物槽位通常是 BOSS
          const isFirst = parseInt(slotKey) === Math.min(...Object.keys(team).map(Number));
          monsterNameDict[id] = isFirst ? bossName : `${bossName}喽啰${slotKey}`;
        }
      }
    }
  }
}
buildMonsterNamesFromCapture();

export function getMonsterName(monsterId) {
  const id = Number(monsterId);
  if (monsterNameDict[id]) return monsterNameDict[id];
  return `怪物#${monsterId}`;
}

// ====== 抓包数据接口 ======
export function getInitialRoomInfo() { return captureData.initialRoomInfo; }
export function getCaptureRoomId() { return captureData._meta?.captureRoomId || captureData.roomId || "28314045"; }
export function getFightSnapshotCount() { return captureData.fightSnapshots?.length || 0; }
export function getFightSnapshot(fightIndex) {
  const snapshots = captureData.fightSnapshots || [];
  if (fightIndex < 0 || fightIndex >= snapshots.length) return null;
  return snapshots[fightIndex].roomInfo || null;
}
export function getFightRoleId(fightIndex) {
  const snapshots = captureData.fightSnapshots || [];
  if (fightIndex < 0 || fightIndex >= snapshots.length) return null;
  return snapshots[fightIndex].roleId;
}

export const mockBattleMeta = {
  roomId: captureData._meta?.captureRoomId || "28314045",
  totalFights: captureData.fightSnapshots?.length || 0,
  fightSequence: captureData.fightSequence || [],
};

export const mockGetRoleInfoResp = {
  nightMareData: {
    roomId: captureData._meta?.captureRoomId || "28314045",
  },
};
```

---

## 7. 批量集成

**文件：** `views/BatchDailyTasks.vue`

### 7.1 十殿 Tab UI

```html
<n-tab-pane name="nightmare" tab="十殿">
    <n-space>
        <n-button size="small" type="warning" @click="batchNightmareChallenge"
                  :disabled="isRunning || selectedTokens.length === 0">
            十殿阎罗挑战
        </n-button>
        <n-button size="small" @click="batchStarChallenge"
                  :disabled="isRunning || selectedTokens.length === 0">
            一键十殿星级挑战
        </n-button>
        <n-button size="small" @click="batchNightmareLottery"
                  :disabled="isRunning || selectedTokens.length === 0">
            一键十殿抽奖
        </n-button>
    </n-space>
</n-tab-pane>
```

### 7.2 batchNightmareChallenge

```js
const batchNightmareChallenge = async () => {
  if (selectedTokens.value.length === 0) { message.warning("请先选择一个账号作为队长"); return; }
  if (selectedTokens.value.length > 1) { message.warning("请只选择一个队长执行"); return; }
  const tokenId = selectedTokens.value[0];
  // 自动连接（轮询30次×500ms）
  if (tokenStore.getWebSocketStatus(tokenId) !== "connected") {
    tokenStore.selectToken(tokenId, true);
    let retries = 0;
    while (tokenStore.getWebSocketStatus(tokenId) !== "connected" && retries < 30) {
      await new Promise((r) => setTimeout(r, 500)); retries++;
    }
  }
  // 跳转游戏功能页面，自动打开十殿挑战
  router.push({ path: "/admin/game-features", query: { openNightmare: "true", tokenId } });
};
```

---

## 8. WebSocket 指令注册

**文件：** `utils/xyzwWebSocket.js`

```js
.register("nightmare_getroleinfo")          // 获取角色十殿信息（roomId）
.register("nightmare_getroominfo")          // 获取战斗房间信息
.register("nightmare_fight")                // 出战
.register("nightmare_leadercomplete")       // 队长确认战斗结束
.register("nightmare_claimweekreward")      // 领取周奖励
.register("nightmare_claimturnrewardtimes") // 领取转盘奖励
.register("nightmare_clickturntable")       // 点击转盘
.register("nightmare_claimbook")            // 领取图鉴
.register("nightmare_restore")              // 恢复成员

// 响应映射
nightmare_getroleinforesp: "nightmare_getroleinfo",
nightmare_getroominforesp: "nightmare_getroominfo",
nightmare_fightresp: "nightmare_fight",
nightmare_leadercompleteresp: "nightmare_leadercomplete",
nightmare_claimweekrewardresp: "nightmare_claimweekreward",
nightmare_claimturnrewardtimesresp: "nightmare_claimturnrewardtimes",
```

**文件：** `utils/gameCommands.js`

```js
nightmare_getroleinfo(ack = 0, seq = 0, params = {}) {
  return { cmd: 'nightmare_getroleinfo', ack, seq, ...params };
}
```

---

## 9. 指令流程汇总

| 指令 | 用途 | 使用位置 |
|------|------|----------|
| `role_getroleinfo` | 获取角色 roleId | GameStatus / NightmareChallengeCard / connectAndDo |
| `nightmare_getroleinfo` | 获取 roomId（判断是否战斗中） | GameStatus 智能导航 / startBattle |
| `matchteam_getroleteaminfo` | 检查已有队伍 | NightmareChallengeCard onMounted |
| `matchteam_getteaminfo` | 获取队伍详情 | NightmareChallengeCard 初始化/刷新 |
| `matchteam_getrandteamlist` | 获取战斗阵容 | createRoom / joinRoom / joinAndReady |
| `matchteam_create` | 创建组队房间 | NightmareChallengeCard createRoom |
| `matchteam_join` | 加入房间 | joinRoom / joinAndReady |
| `matchteam_memberprepare` | 队员准备 | joinAndReady / prepareSelected / prepareAll |
| `matchteam_openteam` | 开始战斗（队长） | NightmareChallengeCard startBattle |
| `matchteam_dismiss` | 解散房间 | NightmareChallengeCard dismissRoom |
| `nightmare_getroominfo` | 获取战斗房间信息 | NightmareBattle fetchRoomInfo |
| `nightmare_fight` | 成员出战 | NightmareBattle startAttack |
| `nightmare_leadercomplete` | 队长确认战斗结束 | NightmareBattle（10秒后） |
| `nightmare_restore` | 恢复成员武将 | NightmareBattle memberRecover / autoAttack |

---

## 10. 涉及文件清单

| 文件路径 | 说明 |
|----------|------|
| `router/index.js` | NightmareBattle 路由定义 |
| `components/GameStatus.vue` | 入口卡片 + Modal + 智能导航 |
| `components/cards/NightmareChallengeCard.vue` | 核心组队页面（1745行） |
| `components/cards/NightmarePreset.vue` | 一键挑战预设系统（655行） |
| `views/NightmareBattle.vue` | 实时战斗页面（1513行） |
| `utils/nightmareBattleMock.js` | Mock 数据映射（179行） |
| `utils/nightmareBattleMockData.json` | 抓包原始数据 |
| `utils/xyzwWebSocket.js` | WebSocket 指令注册 |
| `utils/gameCommands.js` | 指令定义 |
| `views/BatchDailyTasks.vue` | 批量集成入口 |
